// Fill these in from your Supabase project (Settings → API), then reload.
// Both values are safe to publish — the anon key only works through the
// row-level-security rules in schema.sql, which limit every request to
// the signed-in user's own boards.
window.TRACE_CONFIG = {
  url:     "",   // e.g. https://abcdefghijklm.supabase.co
  anonKey: ""    // the "anon / public" key, a long string starting with ey...
};
